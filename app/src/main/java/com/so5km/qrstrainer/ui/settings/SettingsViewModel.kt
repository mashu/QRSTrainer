package com.so5km.qrstrainer.ui.settings

import androidx.lifecycle.ViewModel

class SettingsViewModel : ViewModel() {
    // This is a simple ViewModel for the Settings fragment
    // Currently it doesn't contain any specific functionality
    // but can be extended in the future for managing settings state
} 